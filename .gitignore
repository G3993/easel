build/
# Ignore large pre-built binaries but keep small source/headers
external/ffmpeg-*/
external/opencv/
external/Spout2/
external/cue/

# Local secrets — never commit
.env
.env.*
!.env.example
CMakeCache.txt
CMakeFiles/
deps/
*.zip
.vs/
.vscode/
*.user
CMakeSettings.json
recordings/
*.log
nul

# macOS
.DS_Store
.gstack/

# Python eval virtualenv
.venv-eval/
# Easel project backups
*.bak.*
default.easel.bak.*

# Smithers run store (ephemeral — never commit)
smithers.db
smithers.db-shm
smithers.db-wal
